# State Root

This folder is for generated/runtime state, not source repos.

Recommended layout:
- `state/<project>/dev/`
- `state/<project>/prod/`

Current projects:
- `state/clawtext/dev/`
- `state/clawtext/prod/`
